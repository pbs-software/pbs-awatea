# Export all functions that do NOT begin with a dot (period)
exportPattern("^[^\\.]")

# Export some handy dot functions
export(.compB0.get, .flush.cat)

# Import all packages listed as Imports or Depends
import(
  PBSmodelling, scape, scapeMCMC, lattice, xtable, gdata, gplots
)
#importFrom(gplots, plotCI)
#importFrom(gdata, ll)



