# PBSawatea

# Packages declared in the 'Depends' field should not also be in the 'Imports' field (from R-exts.pdf).
import(methods, scape, scapeMCMC, xtable, gplots)

#importFrom(foo, f, g)
importFrom(PBSmodelling, view)

#import(PBSmodelling, scape, scapeMCMC, xtable, gplots)
#PBSmodelling, scape, scapeMCMC, lattice, xtable, gdata, gplots

# Export all functions that do NOT begin with a dot (period)
exportPattern("^[^\\.]")

# Export dot functions by action (explicit list just to keep track)
exportPattern("^\\.comp")
exportPattern("^\\.flush")
exportPattern("^\\.PBS")


