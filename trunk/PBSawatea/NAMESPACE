# Export all functions that do NOT begin with a dot (period)
#exportPattern("^[^\\.]")
#exportPattern("^[^\\*]") # export all objects

# Export all names
exportPattern(".")

# Import all packages listed as Imports or Depends
import(
  PBSmodelling, scape, scapeMCMC, lattice, gdata, xtable
)

